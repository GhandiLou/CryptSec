(() => {
  const panel = document.getElementById('customPanel');
  const panelBtn = document.getElementById('customPanelBtn');
  const closeBtn = document.getElementById('closeCustomPanel');

  // Inputs
  const bgStart = document.getElementById('bgStartColor');
  const bgMid = document.getElementById('bgMidColor');
  const bgEnd = document.getElementById('bgEndColor');
  const matrixSpeed = document.getElementById('matrixSpeedRange');
  const
